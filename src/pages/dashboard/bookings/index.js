import React from "react";

import DashboardLayout from "../../../components/dashboard-layout";

const Bookings = () => {
    return <DashboardLayout>Bookings (to be worked on if needed)</DashboardLayout>;
};

export default Bookings;
