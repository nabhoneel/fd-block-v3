import React from "react";

import DashboardLayout from "../../components/dashboard-layout";

const Profile = () => {
    return <DashboardLayout>Profile (to be worked on if needed)</DashboardLayout>;
};

export default Profile;
